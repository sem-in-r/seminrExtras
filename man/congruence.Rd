% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/feature_congruence.R
\name{congruence}
\alias{congruence}
\alias{print.congruence_analysis}
\alias{summary.congruence_analysis}
\alias{print.summary.congruence_analysis}
\title{Congruence coefficients for a PLS-SEM model}
\usage{
congruence(seminr_model, reliability = c("rhoA", "rhoC", "cronbach", "one"))

\method{print}{congruence_analysis}(x, digits = 3, ...)

\method{summary}{congruence_analysis}(object, ...)

\method{print}{summary.congruence_analysis}(x, digits = 3, ...)
}
\arguments{
\item{seminr_model}{The estimated SEMinR model to report congruence for.}

\item{reliability}{Which reliability estimate to place on the diagonal of the
construct-correlation matrix: \code{"rhoA"} (default, matches SmartPLS),
\code{"rhoC"} (composite reliability), \code{"cronbach"} (Cronbach's alpha), or
\code{"one"} (unity, as permitted by Franke et al. (2021) when reliabilities are
unknown).

Franke et al. (2021, Eq. 2) specify "the reliabilities" without fixing an
estimator, so all four are in specification. They agree wherever a
construct has a well-defined internal consistency and diverge where it does
not: single-item constructs get 1 under \code{"rhoA"}, \code{"rhoC"} and \code{"cronbach"}
alike, whereas Mode B (formative) constructs get exactly 1 under \code{"rhoA"}
— internal consistency being undefined for a composite — while \code{"rhoC"} and
\code{"cronbach"} still compute a value from the indicators. This only ever
affects pairs that involve a Mode B construct, since each column of the
matrix carries only its own construct's reliability.}

\item{x}{A \code{congruence_analysis} object.}

\item{digits}{Number of decimal places to print (defaults to 3).}

\item{...}{Ignored.}

\item{object}{A \code{congruence_analysis} object.}
}
\value{
An object of class \code{congruence_analysis}: a list with the symmetric
matrix of congruence coefficients (\code{congruence}), the construct names
(\code{constructs}), and the reliability estimator used (\code{reliability}).
}
\description{
\code{congruence()} reports the congruence coefficient rc for every pair of
constructs in an estimated model, as a square table read like a correlation
matrix.
}
\details{
The congruence coefficient describes how similarly two constructs relate to
the other constructs in the model. It is the cosine similarity of the two
constructs' columns in the construct-correlation matrix, with reliabilities
placed on the diagonal (Franke et al., 2021, Eq. 2), and is bounded above by
\enumerate{
\item A value near 1 says the two constructs sit in nearly the same position in
the nomological network; a lower value says their correlation profiles
differ.
}

The calculation is deterministic — one pass over the estimated model, no
resampling — so it returns immediately.
}
\section{Interpreting rc}{

rc is an \strong{effect size}, and should be read as a magnitude rather than
against a cut-off. Franke et al. (2021) define congruence as
\emph{proportionality} of the two correlation profiles, phi_XZi = rho * phi_YZi,
where rho need not equal 1: rho = 1 is exact equivalence and rho != 1 is weak
congruence. The paper states that the DIFF and WALD procedures it evaluates
do not transfer to PLS-SEM, and no significance test for rc has been
validated for PLS-SEM since. Report the coefficient and interpret its size;
do not report it as a test result.

Note also that rc is bounded above by 1, so sampling error can only move an
estimate downwards. That asymmetry is a further reason not to read a value
short of 1 as evidence against congruence.
}

\section{Model types}{

\strong{Interaction constructs are excluded} from the construct set entirely, not
merely from the pair list, since Eq. 2 sums over the whole set. A message
names any construct dropped. \strong{Higher-order models are not supported} and
are refused with a warning.
}

\examples{
library(seminr)

corp_rep_mm <- constructs(
  composite("COMP", multi_items("comp_", 1:3)),
  composite("LIKE", multi_items("like_", 1:3)),
  composite("CUSA", single_item("cusa")),
  composite("CUSL", multi_items("cusl_", 1:3))
)

corp_rep_sm <- relationships(
  paths(from = c("COMP", "LIKE"), to = c("CUSA", "CUSL")),
  paths(from = "CUSA", to = "CUSL")
)

corp_rep_pls_model <- estimate_pls(
  data = corp_rep_data,
  measurement_model = corp_rep_mm,
  structural_model  = corp_rep_sm,
  missing = mean_replacement,
  missing_value = "-99"
)

congruence(corp_rep_pls_model)

}
\references{
Franke, G. R., Sarstedt, M., & Danks, N. P. (2021). Assessing
measure congruence in nomological networks. Journal of Business Research,
130, 318-334.
}
